Read the article at:
  {{ARTICLE_PATH}}

Draft 4 tweet candidates for X (Twitter), each from a different angle:
- A · 金句 — quote the strongest single sentence (or short couplet) from the article, optionally with a one-line lead-in
- B · 反差 — sharp "not X, is Y" cognitive flip
- C · 小灾难 — the "every day, most attempts fail, but failure is the data" rhythm
- D · 清单/时间线 — only when the article carries dense material (multiple events, multiple failure modes, before/after, a list of concrete items). MUST be structured: numbered/bulleted list, dated timeline, or "before → after" table-like rows. No flowing prose. Skip D if the article has no such material.

Length rules — data shows the 60–100 char range is a valley; aim for one of the two peaks:
- A, B, C: TARGET ≤ 60 CJK characters — short and punchy travels furthest.
- D: ≥ 100 CJK characters is fine and encouraged when structure earns it; but every line still has to pull weight (no padding). Skip D rather than pad it to length.
- Hard cap on all candidates: ≤ 140 CJK characters (X allows 280 latin = 140 CJK).
- Avoid landing in 60–100 chars on A/B/C — either trim under 60 or restructure as D.

Other hard constraints (apply to all candidates):
- Preserve 王建硕 voice: plain, honest, conversational, family-style metaphors
- NO hashtags, NO @mentions, NO emoji (unless the original article has them), NO marketing tone
- Material MUST come from the article text — do not invent new examples
- NO mp.weixin link, NO "click here", NO call-to-action

Then pick the strongest candidate (most resonance + tightest line).
When it's a close call, prefer A (金句) — a quotable line tends to reach the most people.
When two candidates are comparably strong and both short, pick the SHORTER one.
Pick D over A/B/C only when the structure itself is the payoff (e.g. a timeline that wouldn't land as one sentence).

Write ONLY the chosen tweet text (exactly what should be posted, no quotes, no metadata) to:
  {{OUT_FILE}}

Write the letter (A or B or C or D) to:
  {{ANGLE_FILE}}

Do not output anything else. Just write those two files.
